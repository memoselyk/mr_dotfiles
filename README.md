# tmux config managed with VCSH/mr

## Usage

Add this package to the `mr` repo/config.

This config uses _Oh my tmux_, customization must be done on a local file:

```sh
cp ~/.tmux/.tmux.conf.local ~/.
```
